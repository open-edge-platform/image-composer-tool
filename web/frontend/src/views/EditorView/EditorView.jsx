import React from 'react';

export function EditorView() {
  return (
    <div style={{ padding: '24px', color: 'var(--color-text-secondary)' }}>
      <h2 style={{ color: 'var(--color-text-primary)' }}>Editor (Phase 4)</h2>
      <p>Future placeholder for CodeMirror template editor.</p>
    </div>
  );
}
