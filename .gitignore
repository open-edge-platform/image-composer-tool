# ignore build artifacts, cache files, and temporary files 
downloads/
builds/  
# Anchored to the repo root so it only ignores the runtime package cache,
# not the internal/cache source package.
/cache/
tmp/ 
workspace/
# AI embedding cache
.ai-cache/

# Local editor and generated artifacts
.vscode/
/image-composer-tool
/live-installer
/image-composer-tool.log
